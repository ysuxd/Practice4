<Window x:Class="Practice.MyOrdersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Мои заказы" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Мои заказы" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="10"/>

        <!-- Информация -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBlock x:Name="OrdersCountText" Text="Заказы" FontSize="16"/>
        </StackPanel>

        <!-- Таблица заказов -->
        <DataGrid x:Name="MyOrdersDataGrid" Grid.Row="2" Margin="10"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№ заказа" Binding="{Binding orderid}" Width="80"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding orderdate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Время" Binding="{Binding ordertime, StringFormat=hh\\:mm}" Width="80"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding statusname}" Width="150"/>
                <DataGridTextColumn Header="Кол-во позиций" Binding="{Binding items_count}" Width="100"/>
                <DataGridTextColumn Header="Сумма" 
                                    Binding="{Binding total_amount, StringFormat={}{0:C}}" 
                                    Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="ViewDetailsButton" Content="Просмотреть детали" 
                    Width="150" Height="30" Margin="5" Click="ViewDetailsButton_Click"/>
            <Button x:Name="RefreshButton" Content="Обновить" 
                    Width="100" Height="30" Margin="5" Click="RefreshButton_Click"/>
            <Button x:Name="CloseButton" Content="Закрыть" 
                    Width="100" Height="30" Margin="5" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>